(function() {

  $('input[type="date"]').datepicker({
    format: "yyyy-mm-dd",
    weekStart: 1,
    startDate: "today",
    todayBtn: "linked",
    language: #{toJSON lang},
    calendarWeeks: true,
    autoclose: true,
    todayHighlight: true
  });

})();